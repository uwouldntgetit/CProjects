// how it basically works: the user digits the program name, then it inputs the text strings to sarch between "" symbols. Then the user inputs 
// some filenames where the pattern search will take place (in a later version it could be implemented a way to directly input a directory).
// in the end the program will print all the lines containing any of the regexs

#include "hader.h"

struct vl_matrix* compute_regexs(
